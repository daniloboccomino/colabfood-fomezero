@model Provider

@{
    ViewData["Title"] = "Perfil";
}

<div class="card mb-3" style="max-width: 30rem;">
    <h4 class="card-header">Dados de Cadastro</h4>
    <div class="card-body">
        <form asp-action="Create">
            <partial name="_Form" />
            <input type="submit" value="Cadastrar" class="btn btn-primary mt-4 mb-1" />
        </form>
    </div>
</div>